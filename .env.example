# Optional: only needed when a future ingestion job uses Financial Modeling Prep.
FMP_API_KEY=

# Source toggles. The MVP reads local SQLite/sample data on dashboard refresh.
ENABLE_FMP=false
ENABLE_YAHOO=false
ENABLE_SEC_EDGAR=false

# Optional local override.
# SCREENER_DB_PATH=data/cache/sec_signal_radar.sqlite3
