from __future__ import annotations

import pandas as pd


def load_cached_prices() -> pd.DataFrame:
    """Placeholder for cached market data. The MVP avoids refresh-time API calls."""
    return pd.DataFrame()
